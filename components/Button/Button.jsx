'use client';
import { Btn } from './Button.styles';

export function Button({ hero }) {
	return (
		<Btn hero={hero}>
			Soon ...
			{/* Order a development */}
		</Btn>
	);
}

export default Button;
